os.loadAPI("dc.lua")

shell.run("github", "clone", "dinxton/dcore", "/")
print("Finished executing.")

print("Press Any Key To Continue...")
input = read()
if input == "1" then
  dc.cExit()
else
  dc.cExit()
end
